# Documents the Lambda dependency floors and why they exist. The Makefile carries the
# actual install lists (it needs two pip invocations with different flags) — keep both
# in sync when changing a floor.
google-genai>=1.0.0
feedparser>=6.0.11
# boto3 must be recent enough for sts:GetWebIdentityToken, which backs the Anthropic
# workload-identity-federation path. The Lambda runtime's bundled boto3 predates it,
# so this vendored copy is what makes it available.
boto3>=1.43.0
# anthropic.WorkloadIdentityCredentials — required for the federated auth path.
anthropic>=0.102.0
sqlite-vec>=0.1.0
